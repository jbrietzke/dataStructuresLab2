#include "List.h"

List::List()
{
   *head = NULL;
   numNodes = 0;
}

List::List(List copyingList)
{
   *head = NULL;
   // traverse list copyingList
   // create a copyof each node
   // append new node to new list
}
